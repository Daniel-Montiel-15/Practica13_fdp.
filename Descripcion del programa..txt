Este programa 
1) Lee los datos de un vector (A) de un archivo de texto llamado "vector.txt" 
2) Almacena dichos datos en variables.
3) A traves de operaciones crea un vector (B) perpendicular al vector (A).
